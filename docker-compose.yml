version: '3.3'

services:
  location_monitor:
    build: .
    command: python3 -m src.cloudflare_monitor.monitor
    env_file:
      - .env
    volumes:
      - ./data:/app/data:rw
    restart: unless-stopped
    user: "1001:1001"
    security_opt:
      - no-new-privileges:true

  incident_monitor:
    build: .
    command: python3 -m src.cloudflare_monitor.incident
    env_file:
      - .env
    volumes:
      - ./data:/app/data:rw
    restart: unless-stopped
    user: "1001:1001"
    security_opt:
      - no-new-privileges:true